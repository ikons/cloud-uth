apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    # Expose the database Pod under a stable in-cluster DNS name.
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
