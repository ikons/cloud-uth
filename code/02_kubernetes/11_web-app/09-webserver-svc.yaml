apiVersion: v1
kind: Service
metadata:
  name: webserver-service
spec:
  selector:
    # Publish the web frontend through a stable ClusterIP Service.
    app: webserver
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
