apiVersion: v1
kind: Service
metadata:
  name: web-headless
spec:
  # A headless Service gives stable DNS names to StatefulSet Pods.
  clusterIP: None
  selector:
    app: web-sts
  ports:
    - port: 80
      name: http
