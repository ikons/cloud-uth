apiVersion: v1
kind: Service
metadata:
  name: web-app
spec:
  selector:
    # Route traffic to the Deployment Pods through their shared app label.
    app: web-app
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
